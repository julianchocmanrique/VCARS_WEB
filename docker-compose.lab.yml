services:
  vcars_web_lab:
    build: .
    container_name: vcars-web-lab
    env_file:
      - .env.lab
    environment:
      NEXT_PUBLIC_API_URL: http://187.124.65.93:4000
      API_PROXY_TARGET: http://187.124.65.93:4000
    ports:
      - "0.0.0.0:3010:3000"
    networks:
      - default
      - vcars_api_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3000/vcars/login/').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

networks:
  vcars_api_net:
    external: true
    name: vcars-api_default
